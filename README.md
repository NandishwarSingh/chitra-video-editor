# Chitra Video Editor

A browser-native video editor prototype built with React and Vite.

## Start

```bash
npm install
npm run dev
```

The first version supports local video import, playback, timeline scrubbing, trim handles, in/out marking, thumbnail generation, volume and speed controls, and EDL export.
